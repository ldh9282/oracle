tomcat server 에서는 web-inf/lib 의 클래스는 자동으로 로딩이 안됨...
그래서 메뉴얼로 드라이버 클래스를 로딩하거나... web-inf/lib 대신 다른 폴더에 옮기는 방법이 있음...
jar in $CATALINA_HOME/lib, so the Common class loader can load it automatically.
jar in $JAVA_HOME/jre/lib/ext, so the System class loader can load it automatically.